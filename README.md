# studentmanagementsystem2
install xampp start server apache and mysql

if want new/empty data to the system
you just go to localhost/phpmyadmin -> sql and paste code in zip without_data_new.sql
if want with data
you just go to localhost/phpmyadmin -> create new database -> name 'primary_school_db'
and then import 'with_data_database_name_primary_school_db.sql'

enjoy!
