# studentmanagementsystem2
install xampp start server apache and mysql

if you want empty data to the system
you just go to localhost/phpmyadmin -> sql and paste code in github name:
'database_schema_without_data.sql'

if you want with data
you just go to localhost/phpmyadmin -> create new database -> name:
'primary_school_db' and then import 'database_with_data_name_primary_school_db.sql'

enjoy!